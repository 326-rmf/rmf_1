引入vue
<script src="https://cdn.jsdelivr.net/npm/vue/dist/vue.js"></script>

设置定时器
setInterval()

清除定时器
clearInterval()

:disabled  //有判断条件    :disabled="!mobile || !password" //密码和用户名为空则不可点击

this.selectorItems = [...new Set(this.selectorItems)];//将selectorItems里面的数据去重,这是Set函数的作用

delItem:function(item,index){this.selectorItems.splice(index,1)}
selectorItems是数组,点击按钮触发..来删除数组当中的这一个元素
